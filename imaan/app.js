const DATA=[...(window.IMAAN_UPCOMING||[]),...(window.IMAAN_ONGOING||[])],storageKey='imaanSavedV2';const savedSet=(()=>{try{return new Set(JSON.parse(localStorage.getItem(storageKey)||'[]'))}catch(e){return new Set}})();const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const cardHTML=e=>`<article class="card" data-id="${esc(e.i)}" data-c="${esc(e.c)}"><div class="visual ${esc(e.c)}"><span class="emoji">${e.e}</span><button class="fav${savedSet.has(e.i)?' on':''}" type="button" aria-label="Save ${esc(e.t)}" aria-pressed="${savedSet.has(e.i)}">${savedSet.has(e.i)?'♥':'♡'}</button></div><div class="body"><div class="tags">${e.g.map(t=>`<span class="tag${t[1]?' hot':''}">${esc(t[0])}</span>`).join('')}</div><h3>${esc(e.t)}</h3><div class="meta">${e.m.map(m=>`<div>${m}</div>`).join('')}</div><div class="note">${e.n}</div><div class="cardActions">${e.a.map(a=>`<a class="btn ${esc(a[2])}" target="_blank" rel="noopener" href="${esc(a[1])}">${esc(a[0])}</a>`).join('')}</div></div></article>`;document.querySelector('.collection[data-section="upcoming"] .grid').innerHTML=(window.IMAAN_UPCOMING||[]).map(cardHTML).join('');document.querySelector('.collection[data-section="ongoing"] .grid').innerHTML=(window.IMAAN_ONGOING||[]).map(cardHTML).join('');const cards=[...document.querySelectorAll('.card')],filters=[...document.querySelectorAll('.filter')],sections=[...document.querySelectorAll('.collection')],empty=document.querySelector('#empty'),toast=document.querySelector('#toast');let savedOnly=false;const ping=t=>{toast.textContent=t;toast.classList.add('show');clearTimeout(ping.t);ping.t=setTimeout(()=>toast.classList.remove('show'),1500)};function render(){const f=document.querySelector('.filter.active')?.dataset.f||'all';let total=0;cards.forEach(c=>{const v=(f==='all'||c.dataset.c===f)&&(!savedOnly||savedSet.has(c.dataset.id));c.style.display=v?'flex':'none';if(v)total++});sections.forEach(s=>{const n=[...s.querySelectorAll('.card')].filter(c=>c.style.display!=='none').length;s.hidden=!n;s.querySelector('[data-count]').textContent=n+' idea'+(n===1?'':'s')});empty.style.display=total?'none':'block'}filters.forEach(b=>b.onclick=()=>{filters.forEach(x=>x.classList.remove('active'));b.classList.add('active');render()});document.querySelectorAll('.fav').forEach(b=>b.onclick=()=>{const id=b.closest('.card').dataset.id;savedSet.has(id)?savedSet.delete(id):savedSet.add(id);localStorage.setItem(storageKey,JSON.stringify([...savedSet]));const on=savedSet.has(id);b.classList.toggle('on',on);b.textContent=on?'♥':'♡';b.setAttribute('aria-pressed',on);ping(on?'Saved':'Removed');render()});document.querySelector('#saved').onclick=e=>{savedOnly=!savedOnly;e.currentTarget.textContent=savedOnly?'← Show everything':'♡ Show my favourites';render()};render();
